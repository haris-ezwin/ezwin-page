---
// Get all school logos
const schoolLogos = import.meta.glob("/public/school-logos/*.png");
const numLogos = Object.keys(schoolLogos).length;

// Create array from 1 to number of logos
const logoNumbers = Array.from({ length: numLogos }, (_, i) => String(i + 1));
---

<div class="py-16 sm:py-24 bg-background">
  <div class="mx-auto max-w-7xl px-6 lg:px-8">
    <div class="mx-auto max-w-5xl">
      <div class="grid grid-cols-1 lg:grid-cols-5 gap-12 items-center">
        <!-- Image (2 parts) -->
        <div class="lg:col-span-2 flex justify-center lg:justify-start">
          <div class="relative w-full max-w-xs">
            <div class="w-full aspect-square rounded-lg overflow-hidden bg-gray-200">
              <img
                src="/items/haris-w-kid.jpg"
                alt="Founder"
                class="w-full h-full object-cover"
              />
            </div>
            <div class="absolute -bottom-4 -right-4 bg-primary text-white px-4 py-2 rounded-lg shadow-lg">
              <p class="font-semibold text-base">7 Years</p>
              <p class="text-xs">Teaching Experience</p>
            </div>
          </div>
        </div>

        <!-- Content (3 parts) -->
        <div class="lg:col-span-3">
          <h3 class="text-2xl font-bold text-gray-900 mb-4">
            About the Founder
          </h3>
          <div class="space-y-4 text-gray-600">
            <p>
              <strong>Haris Samingan</strong> brings 7 years of teaching experience and a unique background as an Ex-Google Developer Expert. He has taught coding at prestigious institutions including NTU, SMU, ACS(I), MGS, and Singapore Sports School, receiving positive feedback from both students and teachers.
            </p>
            <p>
              At the start of 2025, Haris transitioned to teaching mathematics, combining his technical expertise with his passion for education. His mission at EzWin Academy is to build the future of education.
            </p>
            <p>
              To achieve this, he is developing an innovative online system that helps students improve their marks and grades through online tools and analytics, moving beyond the traditional "more papers, better results" approach to provide targeted, data-driven learning.
            </p>
          </div>

        </div>
      </div>

      <!-- School Logos - spanning full width -->
      <div class="mt-16 lg:col-span-5">
        <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-8 items-center">
          {
            logoNumbers.map((num) => (
              <div class="flex justify-center">
                <img
                  src={`/school-logos/${num}.png`}
                  alt={`School Logo ${num}`}
                  class="h-16 w-auto object-contain grayscale hover:grayscale-0 transition-all duration-200"
                />
              </div>
            ))
          }
        </div>
      </div>
    </div>
  </div>
</div>
